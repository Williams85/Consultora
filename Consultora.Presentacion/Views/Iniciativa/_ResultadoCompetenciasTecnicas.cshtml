@model List<Consultora.Entidad.CompetenciaTecnicaEntidad>
    @{
        ViewBag.Title = "_ResultadoCompetenciasTecnicas";
    }

    @if (Model != null && Model.Count > 0)
    {
        <table>
            <thead>
                <tr>
                    <th></th>
                    <th>Competencia Tecnica</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var entidad in Model)
                {
                    <tr>
                        <td class="text-center">
                            <input type="radio" name="competenciatecnica" data-id="@entidad.Cod_Competencia_Tecnica" class="ckbcomtec" />
                        </td>
                        <td>
                            @entidad.Nom_Competencia_Tecnica
                        </td>
                    </tr>
                }

            </tbody>
        </table>
    }




